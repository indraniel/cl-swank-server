#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(repl-server) :silent t))

(defpackage :ros.script.ros-swank.3852373250
  (:use :cl))
(in-package :ros.script.ros-swank.3852373250)

(defun main (&rest argv)
  (repl-server:swank-repl-server argv))

;;; vim: set ft=lisp lisp:
